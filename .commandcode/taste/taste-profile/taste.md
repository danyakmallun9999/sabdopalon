# Taste Profile
- User writes in Indonesian (Bahasa Indonesia); responses should be in Indonesian. Confidence: 0.9
- When a bug is reported, proactively audit the rest of the codebase for similar bugs/errors and fix them in the same pass, not just the reported instance. Confidence: 0.85
- Wants the codebase analyzed first and a written plan (with concrete design decisions) before a feature is implemented — explicitly requested "planning" for the one-click install feature, and when handing over new TODO items explicitly asked to re-check the codebase ("cek codebase kembali") to map actual DONE/PARTIAL/NOT DONE status before updating the plan. Confidence: 0.85
- Prioritizes beginner-friendliness above all — "user benar2 dimanjakan" (users should be truly pampered): zero-friction one-click install, automatic folder/config creation, safe defaults that just work; benchmarks against XAMPP/Laragon/Laravel Herd and targets users new to programming. Confidence: 0.9
- Wants a native desktop app with the best per-OS UX rather than one-size-fits-all: Windows should be fully GUI (user never touches the CLI), while macOS/Linux may still involve CLI during installation. Confidence: 0.9
- Chose Tauri v2 as the desktop shell technology (thin Rust shell wrapping the existing Go core as a sidecar) and wants the desktop app to include its own GUI setup wizard, not just the CLI first-run wizard. Confidence: 0.75
- The desktop app must be a true desktop app, not a web page in a browser: UI renders in the app's own native window via webview (WebView2/WebKit) with no URL bar and the browser never opens by default, though tray/menu keeps an "Open in Browser" option for power users. Confidence: 0.8
- Prefers an XAMPP-style distribution model over on-demand fetching: the core stack (PHP + MariaDB) should ship/install by default so a fresh install is immediately usable, while heavier optional extras (PostgreSQL) stay one-click opt-in; primary target users are PHP developers (WordPress/Laravel). Confidence: 0.8
- Cares about forward-looking architecture extensibility: proactively asks whether the design will make future additions (e.g., nginx/apache, phpMyAdmin) cheap to add, and chose to bake design seams into the plan now (proxy backend interface, reserved "system site" hosts, services path) without delivering those new features in the current scope — keeps the current scope tight while preparing for future features. Confidence: 0.8
- When offered a choice between a simpler/native implementation and a more polished in-app UX, picks the most polished option even at higher development cost: chose an embedded xterm.js terminal (PTY + WebSocket in Go) over an OS-native terminal window for the Laragon-style "Open Terminal" feature, explicitly relaxing the earlier "no new Go dependencies" rule to make it possible. Confidence: 0.7
- Runs feature work against a TODO checklist (S1–S5) and expects remaining items to be picked up and completed, with each item verified against real code state (DONE/PARTIAL/NOT DONE) rather than assumed. Confidence: 0.8
- Expects docs (README, CHANGELOG, DESIGN) and version bumps to be updated as part of the same work, not deferred — the release checklist explicitly includes "README Services section + CHANGELOG + tag + rilis". Confidence: 0.7
- Wants to be asked before committing: after implementation, asks "Mau saya commit semuanya ... atau Anda mau review dulu?" and leaves the decision to the user. Confidence: 0.75
- Prefers pragmatic platform-specific solutions: bundles a Windows-only binary port for Redis (tporadowski) while Linux/macOS fall back to the system redis-server via PATH, with a clear CLI hint pointing to the package manager — no attempts to force a universal binary. Confidence: 0.7
- Prefers autonomous execution once a plan is agreed: explicitly opts to run with `--yolo` (skip per-step confirmations / auto-accept) when implementing an already-planned feature, rather than being asked for confirmation at each step. Confidence: 0.7
- Expects plans to be persisted to disk (e.g., `~/.commandcode/plans/`) so a previously-made plan can be retrieved and resumed in a later session; expects the agent to remember prior plans across sessions ("kamu masih tahuu kan? coba review dulu") and to re-review codebase state against the plan before executing. Confidence: 0.7
- Prefers the full build to be verified locally on the actual machine rather than deferred to CI: when told the Tauri desktop build couldn't be verified because Rust wasn't installed, the user asked the agent to install the Rust toolchain on the system and then compile and test locally ("tolong install rust pada sistem saya ya, lalu lakukan kompilasi dan testing") — closing the verification loop by installing missing toolchains as needed; in later sessions also asks for a fresh local compile of the Linux build so they can try it themselves ("tolong compile untuk linux di lokal ya, mau saya coba"), and expects the exact artifact location (deb/rpm/AppImage paths) to be reported so they can run it. Confidence: 0.9
- When the agent cannot run privileged commands (sudo requires a password), the user is willing to run the exact commands provided in their own terminal and confirm briefly ("oke sudah"), expecting the agent to verify the result and continue automatically; prefers installing all required dependencies for the complete build (tray icon + AppImage) over proceeding with a degraded build. Confidence: 0.75
- Expects the finished product to be verified end-to-end on the real machine with the real GUI stack — including launching the app on the actual display, confirming the window appears, exercising the full first-run wizard flow via API, verifying the proxy serves a created site, and confirming the WebSocket terminal handshake — then cleanup of test processes, and finally rebuilding fresh bundles with the corrected code; a successful compile alone is not enough. Confidence: 0.8
- Keeps a single source of truth for version numbers: when the desktop bundle's version (Cargo.toml, tauri.conf.json, package.json, Cargo.lock) drifted from the Go app version, the user expected them to be synchronized; the agent proactively aligned all version fields to the release version (0.7.0) before finalizing artifacts. Confidence: 0.75
- Explicitly specifies UI conventions in feature requests and expects the agent to follow them rather than improvise the layout — e.g., the Sites-page terminal must be a separate, resizable column (split-pane with a drag handle) instead of a popup/sheet overlay, and earlier specified a right-side panel using shadcn components. Confidence: 0.8
- Wants UI revamps built on shadcn components with a polished look and feel — "revamp ui ux dengan bagus dengan shadcn" — shadcn is the go-to UI toolkit for new pages/revamps rather than hand-rolled markup; explicitly re-stated for the setup wizard too ("bagian wizard juga revamp ya ui ux, gunakan komponen shadcn seperti progress dll"). Confidence: 0.85
- Expects status/data shown across different pages to be consistent and accurate — when the user spots a discrepancy (e.g., Sites shows a service running but the Dashboard shows none), the difference should be investigated to its root cause: distinguish genuine conceptual differences (site PHP status vs optional managed services) from real bugs (services API returning null when none were enabled), fix real bugs in the backend so every view agrees, and explain the difference plainly. Confidence: 0.7
- Expects the UI to be responsive across viewports (mobile/tablet/desktop), not just desktop — flagged the Sites page as "kurang responsive" once the resizable terminal split-pane was added and asked for a responsive revamp as part of the same work. Confidence: 0.7
- Bundles related polish/refactors into the same feature request rather than separate tickets — "sekalian revamp halaman site karena kurang responsive" (revamp the Sites page responsiveness in the same pass as the new Dashboard feature). Confidence: 0.6
- Expects the release pipeline to produce ready-to-install artifacts for all target OSes, not just the developer's platform — asked whether GitHub Actions CI builds Windows/macOS/Linux installers as part of the release definition, and later independently checked the GitHub Releases page and reported the missing v0.7.0 release ("file release-nya kok gak ada? masih yang v0.5.0 di github"). A "release" means actually published artifacts from a working tag-triggered workflow, not just a commit/tag pushed. Confidence: 0.85
- Expects runtime configuration (php.ini) to be user-editable per install with sensible defaults and documentation — approved adding `config/php.ini` (via PHPRC) rather than relying on the bundled binary's baked-in defaults. Confidence: 0.6
- Wants CI/CD to be green before work is considered done: when asking to push, explicitly said "pastikan ci cd tidak ada yang error" (make sure CI/CD has no errors) — expects the agent to audit workflow files for latent failure points before pushing (e.g., gitignored lockfiles breaking `npm ci`, version string `v`-prefix bugs, cross-compile target mismatches), fix them, and then monitor the CI run to completion after push and report the outcome instead of just pushing and hoping. Confidence: 0.85
- Wants README/docs written in plain, non-technical language that a beginner can follow ("ubah agar bahasanya tidak terlalu teknis") — rewrite jargon-heavy or English technical prose into simple friendly wording, even when the audience is developers. Confidence: 0.9
- Does not want the README/docs to compare the product against other tools — explicitly asked to drop the Laragon/XAMPP/Herd comparison table and "alternative to X" framing ("tidak perlu membandingkan dengan tools lain"). Confidence: 0.9
- Expects leftover build/test artifacts to be cleaned up after a task is done — explicitly asked to delete "sisa-sisa build test yang sudah tidak diperlukan" (temp logs, simulated packaging dirs, debug strace logs, temp icons) while keeping genuinely useful outputs (final deb/rpm/AppImage bundles and the gitignored sidecar binary), and wants the cleanup confirmed/explained. Confidence: 0.7
- Prefers to manually test a build/version himself before the agent proceeds with further work (rebuild, release, etc.) — when offered a rebuild of the latest AppImage, replied "jangan dulu, saya testing manual dulu yang versi ini" (not yet, I'll test this version manually first); the agent should hold off on further builds/releases until the user asks. Confidence: 0.6
- Dashboard/UI should only show tools that are actually installed — explicitly stated that tools not yet downloaded/installed in the system must not appear in the dashboard with a "Start" button; only installed tools get shown with start controls, uninstalled ones are hidden entirely rather than listed disabled; the same installed-only filtering (with an empty state pointing to Packages) was applied to the Services page too. Confidence: 0.9
- Service start/stop controls should be runtime-only operations with immediate visible effect, not config toggles — asked what "Start All" was for after clicking Start left the status unchanged; the fix separates runtime start/stop endpoints from the persisted enable flag so Start/Start All genuinely changes the running status on screen without rewriting config. Confidence: 0.65
- Wants a clear, non-confusing data-flow separation between concepts that sound similar — pushed back on the ambiguity between Site start/stop (the PHP process of one project) and Service start/stop (optional tools like Mailpit/Redis/MinIO), and approved a design where runtime Start/Stop, persisted Auto-start preference, and site control are three distinct, clearly-labeled things; UI labels should use plain terms ("Auto-start" not "Enabled") so the semantics are obvious at a glance. Confidence: 0.75
- Installed tools should start automatically when the app launches (auto-start ON by default) — "buka aplikasi langsung nyala semua": fresh configs and newly installed service packages default to auto-start ON, and the app starts every installed+enabled service at serve time; users can still switch any service off via the Auto-start toggle. Confidence: 0.8
- Startup/runtime failures must be surfaced in the UI, not silently logged — when a service failed to auto-start due to a port conflict, the agent initially only printed the error to the console; the user's workflow required the dashboard to show an error banner with the actual reason (e.g., "port 9001 busy"), so the backend records a per-service last_error exposed in the API and displayed on cards/pages. Confidence: 0.7
- Expects the full workflow to be completed autonomously once direction is clear — after the user approved "auto-start ON + revamp Services page", the agent was expected to implement backend+frontend, verify end-to-end in a browser, commit, push, build the local Linux AppImage, update the release tag, and monitor the CI/release workflows to completion without further check-ins. Confidence: 0.8
