# Per-project Sabdopalon configuration.
# Overrides global config (config/engine.toml) for THIS project only.

php: "8.4"
node: "22"
database: "mysql"
docroot: "public"
aliases:
  - www.example-app.localhost
env:
  APP_ENV: local
  APP_DEBUG: "true"
